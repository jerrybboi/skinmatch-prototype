import './globals.css';

export const metadata = {
  title: 'SkinMatch',
  description: 'Personalized skincare assessment prototype'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
